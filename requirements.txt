# A股多策略模拟交易系统 —— 运行依赖
#
# ⚠ 这份清单必须与 pyproject.toml 的 [project].dependencies 保持一致。
#   保留它是为了不装包也能直接 pip install -r 起一套运行环境（调度机上的用法）；
#   开发环境请用 `make install`（等价于 pip install -e ".[dev]"），
#   会一并装上 pytest / ruff / mypy。
# 标准库以外仅需以下两个第三方包。
# 安装：pip install -r requirements.txt
#
# 说明：
#  - akshare 提供行情/新闻数据源（market.py 多源交叉验证、news_feed.py 个股新闻）。
#    当前代码依赖 stock_news_em；已在 news_feed._default_fetch 中用
#    pd.set_option("mode.string_storage","python") 规避某些 akshare 版本的
#    pandas/pyarrow str.replace 崩溃。若你的 akshare 版本行为不同，按注释调整。
#  - pandas 为 akshare 依赖，同时被 news_feed / strategy 直接使用。
#  - numpy 通常随 pandas 自动安装，这里显式列出以防最小环境缺失。

akshare>=1.12.0
pandas>=1.5.0
numpy>=1.23.0
